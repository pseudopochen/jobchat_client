import React, { Component } from "react";
import { connect } from "react-redux";

class Manager extends Component {
  render() {
    return <div></div>;
  }
}
export default connect((state) => ({}), {})(Manager);
