import React, { Component } from "react";
import { connect } from "react-redux";

class Applicant extends Component {
  render() {
    return <div></div>;
  }
}

export default connect((state) => ({}), {})(Applicant);
